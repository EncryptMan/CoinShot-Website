import { Anton, Varela_Round } from "next/font/google"


export const anton = Anton({
    subsets: ['latin', 'latin-ext'],
    weight: ['400', '400'],
})

export const verala = Varela_Round({
    subsets: ['latin', 'latin-ext'],
    weight: ['400'],
})